<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Track what you will</title>
		<link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Bitter:400,700">
		
		{{#if isDev}}
		<link rel="stylesheet" type="text/css" href="/css/styles.css">
		{{else}}
		<link rel="stylesheet" type="text/css" href="/css/styles.min.css">
		{{/if}}
	</head>

	<body class="jumbotron">
		
		<img id="ribbon" src="/img/header.png" alt="Mitch's birf day tweets"></img>
		
		<div id="content">
			<form>
				<label>
					You can track whatever you liiiike:
					{{#if curTrack}}
					<input id="text-to-track" type="text" value="{{curTrack}}" maxlength="10">
					{{else}}
					<input id="text-to-track" type="text" value="nasa" maxlength="10">
					{{/if}}
				</label>
				<input id="track" type="submit" value="Track">
				<p class="description">
					Note: This website only tracks one subject at a time.  Meaning if you choose to track turtles, now everyone visiting this 
					website gets to see all twitter turtle goodness.  Don't worry though, it's fun that way.
				</p>
			</form>
			<div id="tweets">
				<p class="initial explanation">
					{{#if curTrack}}
					Currently tracking {{curTrack}}
					{{else}}
					Not tracking anything
					{{/if}}
				</p>
			</div>
		</div>

		<div id="curtrack" data-curtrack="{{curTrack}}"></div>
		{{#if isDev}}
		<script src="/scripts/scripts.browser.js"></script>
		{{else}}
		<script src="/scripts/scripts.browser.min.js"></script>
		{{/if}}
	</body>

</html>
